# Изисква Python 3.10+
# Инсталация:
#   python3.12 -m venv tools/.venv
#   source tools/.venv/bin/activate
#   pip install -r tools/requirements.txt
cursor-sdk>=1.0.24
python-dotenv>=1.0.0

# claude_board_runner.py — носи вграден Claude Code CLI, без нужда от npm/Node.js.
claude-agent-sdk>=0.2.128
anyio>=4.0.0
